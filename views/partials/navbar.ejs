<div class="my-navbar">
  <div class="container">
    <div class="row">

      <div class="my-navbar-header">
        <button type="button"
                class="navbar-toggle collapsed"
                data-toggle="collapse"
                data-target="#navbar"
                aria-expanded="false"
                aria-controls="navbar"
                onclick="displayVerticalMenu()">
          <span class="sr-only">Toggle navigation</span>
          <span>Menu</span>
          <span class="glyphicon glyphicon-triangle-bottom"></span>
          <!--span class="glyphicon glyphicon-option-vertical"></span-->
        </button>
        <a class="nav-brand" href="index"><img class="logo" src="favicon.ico" alt="logo of a hand holding a plant">Going Green</a>

      </div>

      <div id="navbar" class="navbar-collapse collapse">
          <a href="index" class="nav-menu"><%= __('Home') %></a>
          <a href="about_EN" class="nav-menu"><%= __('About') %></a>     <!-- about generic -->
          <a href="design" class="nav-menu"><%= __('Design') %></a>
          <a href="settings" class="nav-menu"><%= __('Settings') %></a>
          <a href="about_FR" class="nav-menu">                           <!-- changeLanguage -->
              <% if (selectedLang === 'en') { %>
                  <img class="icon-flag" src="images/flag-french.jpg">français</a>
                  <% } else { %>
                  <img class="icon-flag" src="images/flag-english.jpg">English</a>
                  <% } %>
      </div>

    </div>
  </div>
</div>

